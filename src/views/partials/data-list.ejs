<div class="flex-grow lg:w-2/3 xl:w-3/4 flex flex-col gap-6 order-first lg:order-none">

    <!-- Top actions & Title -->
    <div
        class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-4 border-b border-slate-200 dark:border-white/10 pb-5">
        <div>
            <h1 class="text-2xl font-bold tracking-tight text-slate-900 dark:text-white flex items-center gap-3">
                <span id="listTitle">Catálogo</span>
                <span id="recordCount"
                    class="text-xs px-2.5 py-0.5 rounded-full bg-slate-100 dark:bg-white/10 text-slate-600 dark:text-slate-300 font-bold border border-slate-200 dark:border-white/5">0</span>
            </h1>
            <p id="listDescription" class="text-sm text-slate-500 dark:text-slate-400 mt-1">Explora y administra tu
                biblioteca.</p>
        </div>

        <!-- Sleek Modern Search -->
        <div class="relative w-full sm:w-72 lg:w-80 group">
            <svg class="w-4 h-4 absolute left-4 top-1/2 -translate-y-1/2 text-slate-400 dark:text-slate-500 group-focus-within:text-indigo-600 dark:group-focus-within:text-indigo-400 transition-colors"
                fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                    d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
            </svg>
            <input type="text" id="searchInput" onkeyup="filterData()" placeholder="Buscar..."
                class="w-full bg-white dark:bg-[#18181b] border border-slate-200 dark:border-white/10 rounded-xl py-2.5 pl-11 pr-4 text-sm focus:outline-none focus:border-indigo-500 dark:focus:border-indigo-500 focus:ring-1 focus:ring-indigo-500 dark:focus:ring-indigo-500 transition-all placeholder-slate-400 dark:placeholder-slate-500 text-slate-900 dark:text-slate-100 shadow-sm">
        </div>
    </div>

    <!-- Loading State -->
    <div id="tableLoader" class="py-32 flex flex-col items-center justify-center">
        <svg class="animate-spin w-8 h-8 mb-4 text-indigo-500 dark:text-indigo-400" fill="none" viewBox="0 0 24 24">
            <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
            <path class="opacity-75" fill="currentColor"
                d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z">
            </path>
        </svg>
        <span class="text-xs uppercase tracking-widest font-bold text-slate-500 dark:text-slate-400">Cargando...</span>
    </div>

    <!-- Data Grid max density full width -->
    <div id="dataListContainer" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 xl:grid-cols-3 gap-5 hidden">
        <!-- Cards dynamically inserted -->
    </div>

</div>